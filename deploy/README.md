# LLSpam Deployment

1. Copy the example .env and apikeys file into this directory, remove .example extension.
2. Add API key to .env file
3. Modify port (if desired) in .env file
4. Generate API keys (list of Guids) and enter into apikeys.txt
- https://it-tools.tech/uuid-generator
5. Run `docker compose up -d`
